[build]
cmd = "go build -o ./tmp/main.exe ./cmd/api"
bin = "tmp/main.exe"

include_ext = ["go"]

exclude_dir = [
  "tmp",
  "vendor",
  ".git",
  "node_modules"
]

exclude_regex = ["_test.go"]

exclude_unchanged = true

delay = 500
stop_on_error = true